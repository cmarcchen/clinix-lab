import { queries } from "./queries.js";
import { mutations } from "./mutations.js";
import { resolvers } from "./resolvers.js";
import { types } from "./types.js";

export const PatientEvent = { queries, mutations, resolvers, types };
