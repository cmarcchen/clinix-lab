query GetPatients {
  patients {
    id
    firstName
    lastName
    age
    email
  }
}

mutation CreatePatient($data: PatientInput) {
  createPatient(data: $data) {
    code
    message
    success
    patient {
      id
      createdAt
      firstName
      lastName
      age
      email
      sex
    }
  }
}
